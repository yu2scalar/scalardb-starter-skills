# Generated by scalardb-starter-skills/scalardb-generate-config
# Client properties for the ScalarDB Java (CRUD) API, indirect mode via Envoy.
#
# {{CLIENT_HOST_COMMENT}}

scalar.db.transaction_manager=cluster
scalar.db.contact_points=indirect:{{CLIENT_CONTACT_HOST}}
scalar.db.contact_port=60053
{{#if AUTH_ENABLED}}

# Authentication (default superuser is admin/admin — change it after first login)
scalar.db.cluster.auth.enabled=true
scalar.db.username={{AUTH_USERNAME}}
scalar.db.password={{AUTH_PASSWORD}}
{{/if}}
